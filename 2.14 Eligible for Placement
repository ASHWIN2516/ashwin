#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    char name[50];
    int arrears;
    float cgpa;

    scanf("%s %d %f", name, &arrears, &cgpa);

    if ((arrears == 1 && cgpa > 70) || (arrears <= 2 && cgpa > 75))   {
        printf("Name of the Student:%s\n", name);
        printf("%s is Eligible for Placement\n", name);
    } else  {
        printf("Name of the Student:%s\n", name);
        printf("%s is Not Eligible for Placement\n", name);
    }
    
    return 0;
}
